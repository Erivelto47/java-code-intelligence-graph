# Validation Checklist: phase-4-3-1-java-mixed-throw-return-if-else

## Runner metadata

- Phase id: `phase-4-3-1-java-mixed-throw-return-if-else`
- Primary blueprint path: `harness/blueprints/phase-4-3-1-java-mixed-throw-return-if-else.blueprint.md`
- Handoff path: `harness/handoffs/phase-4-3-1-java-mixed-throw-return-if-else.handoff.md`
- Validation path: `harness/validations/phase-4-3-1-java-mixed-throw-return-if-else.validation.md`
- Completion path: `harness/completion/phase-4-3-1-java-mixed-throw-return-if-else.completion.md`
- Runtime report path: `harness/reports/runs/PHASE_4_3_1_JAVA_MIXED_THROW_RETURN_IF_ELSE_REPORT.md`
- Generated by: harness blueprint runner

This checklist is a derived, reviewable artifact. The primary blueprint is the
source of truth. If this checklist conflicts with the primary blueprint, pause
execution and correct this checklist before continuing.

This checklist must validate the approved scope without expanding it. Review
before execution when the phase risk, scope or branch state requires it.

## Primary blueprint path

harness/blueprints/phase-4-3-1-java-mixed-throw-return-if-else.blueprint.md

## Blueprint approval status

Blueprint is the primary execution input for this phase.

## Derived artifact status

Derived from:

- primary blueprint;
- harness validation checklist template;
- project conventions and existing validations.

## Scope guard

Confirm this checklist validates the blueprint scope and does not expand it.
If this checklist conflicts with the primary blueprint, pause execution and
correct the checklist before continuing.

## git status

Command:

```bash
git status
```

Result:

## branch check

Command:

```bash
git branch --show-current
```

Result:

## compile/test command

Command:

```bash
./gradlew test
```

Result:

## build command

Command:

```bash
./gradlew build
```

Result:

## relevant CLI command

Command:

```bash
./gradlew run --args="analyze-decisions --project examples/phase-4-decision-trace/01-if-throw-validation --entrypoint com.example.UserService.create"
./gradlew run --args="analyze-decisions --project examples/phase-4-decision-trace/02-if-return-early-return --entrypoint com.example.decisiontrace.ifreturn.ImportService.process"
./gradlew run --args="analyze-decisions --project examples/phase-4-decision-trace/03-unresolved-decision-shapes --entrypoint com.example.decisiontrace.unresolved.RegistrationGuard.validate"
./gradlew run --args="analyze-decisions --project examples/phase-4-decision-trace/04-if-throw-with-pre-statements --entrypoint com.example.decisiontrace.blockthrow.RegistrationGuard.validate"
./gradlew run --args="analyze-decisions --project examples/phase-4-decision-trace/05-single-line-if-throw --entrypoint com.example.decisiontrace.singleline.PaymentGuard.validate"
./gradlew run --args="analyze-decisions --project examples/phase-4-decision-trace/06-if-else-return-branches --entrypoint com.example.decisiontrace.ifelse.FeatureToggleDecision.resolve"
./gradlew run --args="analyze-decisions --project examples/phase-4-decision-trace/07-if-else-throw-return-branches --entrypoint com.example.decisiontrace.ifelsemixed.AccessDecision.resolve"
./gradlew run --args="analyze-flow --project examples/java-simple --entrypoint com.company.FooService.processOrder"
```

Result:

## fixture comparison

Command:

```bash
./gradlew test --tests com.codeatlas.examples.DecisionTraceFixtureContractTest
```

Result:

## git diff --check

Command:

```bash
git diff --check
```

Result:

## report generated

Path:

harness/reports/runs/PHASE_4_3_1_JAVA_MIXED_THROW_RETURN_IF_ELSE_REPORT.md

## no unintended files

Confirmation:

## out-of-scope confirmation

Confirm the validation checked that out-of-scope items from the blueprint were
not implemented.

## no master merge

Confirmation:

## no push unless requested

Confirmation:
